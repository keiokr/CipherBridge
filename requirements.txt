# CipherBridge 极简明文桥接版依赖
PyQt6>=6.6,<7
PyYAML>=6.0
requests>=2.31
mitmproxy>=10.0,<12
pycryptodome>=3.20
# passlib/mitmproxy 在部分 Python 3.13 + bcrypt 5.x 环境会启动崩溃；
# sitecustomize.py 已做运行时兼容，这里同时固定上限，重装依赖时更稳定。
bcrypt<5
